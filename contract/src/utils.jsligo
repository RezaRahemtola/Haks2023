#import "errors.jsligo" "Errors"
#import "storage.jsligo" "Storage"

export const assert_is_owner = (store: Storage.t) =>
{
    assert_with_error(Tezos.get_sender() == store.ownerAddress, Errors.not_owner);
}
